# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Editor / OS junk
*.swp
*~
Thumbs.db
ehthumbs.db

# Local backups created by the setup scripts
*.backup

# VitePress docs site
docs/node_modules
docs/.vitepress/dist
docs/.vitepress/cache

# Stray root lockfiles (docs/ has its own package.json + bun.lock)
/package-lock.json
/node_modules
